# Gateway public API credential. Do not reuse it for token-service.
PUSHGO_TOKEN=

# Gateway -> token-service contract. Defaults to Mainland China; remote URLs require HTTPS.
PUSHGO_TOKEN_SERVICE_URL=https://token.pushgo.cn
