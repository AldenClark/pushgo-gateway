# Gateway public API credential. Do not reuse it for token-service.
PUSHGO_TOKEN=

# Gateway -> token-service contract. Remote URLs require HTTPS and a dedicated Bearer token.
PUSHGO_TOKEN_SERVICE_URL=http://127.0.0.1:6766
PUSHGO_TOKEN_SERVICE_AUTH_TOKEN=
